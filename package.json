{
  "name": "unknown-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Unknown-Engine: Autonomous Scientific Law Discovery Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "lint": "cd frontend && npm run lint"
  }
}
